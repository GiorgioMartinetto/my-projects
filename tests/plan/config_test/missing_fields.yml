app:
  name: "Test App"
  host: "127.0.0.1"
  # port is missing
  version: "2.0.0"
  environment: "dev"
  workers: 1
  debug: false

logging:
  level: "INFO"
  # json_format is missing
  file_enabled: false
  file_path: "logs/app.log"
  rotation: "10 MB"
  retention: "7 days"
  backtrace: true
  diagnose: true
  colorize: false
  enqueue: false
  service_name: "test-service"

